Make the following changes:

   - Based on the given images of model question paper, update practice tab, move word practice toone part, and add new sections based on each and every excercise given in model papers, create more type pf practice tools based on that in a way for a studnets who knows 90% english can learn the chapter to write answers to such arabic questions 

   - DO this for every chapter

   - Also make the botttom tab fixed even in movbile view isntaed of shwoing only when I scroll

After applying the changes recreate project_snapshot.txt only for new files and files with changes, each time improve modularity of the program by introducing new component files (only on modified or new files), don't modify system files like pubspec, if we need new packages, or file path cahnges, removal etc, give the command for bash

For new css, add new files (dont edit App css)
