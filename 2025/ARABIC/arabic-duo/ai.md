Make the following changes:

       Redesign:
        - Current design is created for desktop view but our target users are on phone
                * Inspire from duolingo, make it possible to chose section from the top bar
                * Show units as in duolingo and the lesson path
                * Show progress on top (overall progress, like how much user explored)
                * Add an encouraging, unit and lesson end screen

        Fix:
                - THe buttons in lesson screen some of their style is not applied
                - Reduce size of all elements enough for mobile view
                        - Like the match following should be viwed side by side just like in desktop in mobile view instead of two rows 
 

After applying the changes recreate project_snapshot.txt only for new files and files with changes, each time improve modularity of the program by introducing new component files (only on modified or new files), don't modify system files like pubspec, if we need new packages, or file path cahnges, removal etc, give the command for bash

For new css, add new files (dont edit App css)
